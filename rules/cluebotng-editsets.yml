groups:
  - name: ClueBot NG Edit Sets
    rules:
      - alert: EditSetsEndpointDown
        expr: probe_success{instance="cluebotng-editsets.toolforge.org"} == 0
        for: 5m
        annotations:
          summary: Edit Sets Endpoint Is Down
