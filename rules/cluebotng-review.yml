groups:
  - name: ClueBot NG Review Interface
    rules:
      - alert: ReviewInterfaceDown
        expr: probe_success{instance="cluebotng-review.toolforge.org"} == 0
        for: 5m
        annotations:
          summary: Review Interface Is Down
