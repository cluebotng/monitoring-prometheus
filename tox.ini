[tox]
requires = tox>=4
envlist = ruff,black

[testenv]
package = skip
use_current_env = true

[testenv:black]
commands = black --check {posargs:scripts}
allowlist_externals = black

[testenv:ruff]
commands = ruff check {posargs:scripts}
allowlist_externals = ruff
